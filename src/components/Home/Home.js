import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
            <section>
                <Header></Header>
            </section>
        </div>
    );
};

export default Home;